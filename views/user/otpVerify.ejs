<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>OTP Verification</title>
    <!-- Bootstrap CSS -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-T3c6CoIi6uLrA9TneNEoa7RxnatzjcDSCmG1MXxSR1GAsXEV/Dwwykc2MPK8M2HN" crossorigin="anonymous">
    <link rel="stylesheet" href="/css/style.css">
    <style>
        /* Custom styles for the OTP Verification form */
        body {
            background-color: #f4f7fb;
            font-family: 'Rubik', sans-serif;
        }

        .otp-container {
            display: flex;
            justify-content: center;
            align-items: center;
            min-height: 100vh; /* Full height */
        }

        .otp-form {
            background-color: #ffffff;
            padding: 40px;
            border-radius: 8px;
            box-shadow: 0 2px 12px rgba(0, 0, 0, 0.1);
            width: 100%;
            max-width: 500px; /* Maximum width */
        }

        .otp-form h2 {
            text-align: center;
            margin-bottom: 30px;
            font-weight: 700;
            color: #116677;
        }

        .form-control {
            border-radius: 5px;
            margin-bottom: 15px;
        }

        .btn-primary {
            width: 100%;
            background-color: #116677;
            color: white;
            border: none;
            padding: 10px;
            border-radius: 5px;
        }

        .btn-primary:hover {
            background-color: #118f99;
        }

        .invalid-feedback {
            display: block;
            color: red;
        }

        .back-link {
            text-align: center;
            margin-top: 15px;
        }

        .back-link a {
            color: #116677;
            text-decoration: none;
        }

        .back-link a:hover {
            text-decoration: underline;
        }
    </style>
</head>
<body>

  <div class="otp-container">
    <div class="otp-form">
        <h2>OTP Verification</h2>
        <p class="text-center">A verification code has been sent to your email.</p>
        
        <form action="/verify" method="post">
            <input type="hidden" name="email" value="<%= email %>"> <!-- Pre-fill email from backend -->
            <div class="mb-3">
                <label for="otp" class="form-label">Enter OTP</label>
                <input required name="otp" type="text" class="form-control" id="otp" maxlength="4">
                <div class="invalid-feedback" id="otpError"></div>
            </div>
            <button type="submit" class="btn btn-primary">Verify OTP</button>
        </form>

        <div class="back-link">
            <p>Already verified? <a href="/login">Back to Login</a></p>
        </div>
    </div>
  </div>

</body>
</html>
